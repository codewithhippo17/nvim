-- return {
--   "mistricky/codesnap.nvim",
--   build = "make build_generator",
--   config = function()
--     require("codesnap").setup({
--       watermark = "",
--     })
--   end,
-- }

return { "mistricky/codesnap.nvim", enabled = false } -- Add this to disable it safely
